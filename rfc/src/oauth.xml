<?xml version="1.0" encoding="utf-8"?>
<rfc version="3" ipr="trust200902" docName="draft-ietf-mailmaint-oauth-public-05" submissionType="IETF" category="info" xml:lang="en" xmlns:xi="http://www.w3.org/2001/XInclude" indexInclude="true" consensus="true">

<front>
<title abbrev="OAuth Public">OAuth Profile for Open Public Clients</title><seriesInfo value="draft-ietf-mailmaint-oauth-public-05" stream="IETF" status="informational" name="Internet-Draft"></seriesInfo>
<author role="editor" initials="N.M." surname="Jenkins" fullname="Neil Jenkins">
  <organization>Fastmail</organization>
  <address>
    <postal>
    <street>PO Box 234, Collins St West</street>
    <city>Melbourne</city>
    <code>VIC 8007</code>
    <country>Australia</country>
  </postal>
  <email>neilj@fastmailteam.com</email>
  <uri>https://www.fastmail.com</uri>
  </address>
</author>
<author fullname="Ben Bucksch">
    <organization>Beonex</organization>
    <address>
      <email>ben.bucksch@beonex.com</email>
    </address>
</author>
<date year="2026" month="April" day="2"></date>
<area>ART</area>
<workgroup>Mail Maintenance</workgroup>
<keyword>OAuth</keyword>
<keyword>IMAP</keyword>
<keyword>JMAP</keyword>
<keyword>SMTP</keyword>
<keyword>POP</keyword>
<keyword>CardDAV</keyword>
<keyword>CalDAV</keyword>

<abstract>
<t>This document specifies a profile of the OAuth authorization protocol to allow for interoperability between native clients and servers using open protocols, such as JMAP, IMAP, SMTP, POP, CalDAV, and CardDAV. The profile is restricted to native clients, that is, applications installed and run on the end user's device. It deliberately does not support web-based clients, which cannot complete the flow as specified.</t>
</abstract>

</front>

<middle>

<section anchor="introduction"><name>Introduction</name>
<t>This document pulls together several existing standards and uses them to specify a specific OAuth profile, allowing interoperable modern authentication for native clients of open protocols, such as JMAP, IMAP, SMTP, POP, CalDAV, and CardDAV. For these protocols, there are many servers and many clients with no pre-existing relationship, that need to be able to connect. At the moment, the only interoperable way to do so is with a basic username and password, which has many deficiencies from a security standpoint.</t>

<section anchor="native-clients-only"><name>Applicability to Native Clients Only</name>
<t>This profile applies only to native clients: applications that are installed and executed on the end user's device, as described in <xref target="RFC8252" />. Web-based clients, that is, clients that run in a web browser or on a web server, are out of scope for this document.</t>
<t>This restriction is deliberate. The profile allows any client to register with any server without a pre-existing relationship (see <xref target="dynamic-client-registration" />), and there is no way for the server to verify any of the information supplied during that registration. For a native client, this does not introduce a new risk: a malicious application that the user has already installed and run could spoof the user's browser to phish their credentials, or install malware in unsandboxed environments, so allowing it to complete an OAuth flow makes the user no worse off. Indeed, it is more secure, as the alternative is a legitimate client storing the user's password.</t>
<t>Just navigating to a web page, however, is a different security scenario. Allowing arbitrary web-based clients to seamlessly register and obtain authorization would make it substantially easier for phishing sites to gain access to a user's account, by sending the user through a legitimate OAuth flow at the real authorization server and then receiving the resulting tokens.</t>
<t>The profile therefore enforces this restriction by limiting the redirect URIs that may be registered to loopback addresses and private-use URI schemes, which can only be received by software running on the user's device. See <xref target="dynamic-client-registration" /> for the specific requirements and <xref target="security-considerations" /> for further discussion.</t>
</section>

<section anchor="presumptions"><name>Presumptions</name>
<t>This OAuth flow presumes you have a set of one or more resource server endpoints (e.g., for JMAP/IMAP/SMTP/POP/CardDAV/CalDAV), and a common authorization server issuer identifier for all of them. For example, you might have a JMAP session endpoint <tt>https://api.example.com/jmap/session</tt>, an IMAP endpoint <tt>imaps://imap.example.com:993</tt>, and an issuer identifier <tt>https://auth.example.com/acmeinc</tt>.</t>
</section>

<section anchor="getting-the-issuer-identifier"><name>Getting the Authorization Server Issuer Identifier</name>
<t>The endpoints may be discovered via an autoconfiguration mechanism or manual user input. Autoconfiguration may also include the issuer identifier. To allow OAuth configuration for manual input or when the issuer identifier is not included in the autoconfig, HTTP endpoints that support this profile MUST support <xref target="RFC9728">Protected Resource Metadata</xref>, accessible via the <tt>oauth-protected-resource</tt> well-known path, as specified in <xref target="RFC9728" section="3" />. The <tt>authorization_servers</tt> property MUST be present in the resource metadata, with the value containing the authorization server issuer identifier. HTTP requests made without valid authentication to the resource endpoints MUST indicate the protected resource metadata URL in the <tt>WWW-Authenticate</tt> HTTP response header field, as described in <xref target="RFC9728" section="5" />.</t>
</section>

</section>

<section anchor="notational-conventions"><name>Notational Conventions</name>
<t>The key words "MUST", "MUST NOT", "REQUIRED", "SHALL", "SHALL NOT", "SHOULD", "SHOULD NOT", "RECOMMENDED", "NOT RECOMMENDED", "MAY", and "OPTIONAL" in this document are to be interpreted as described in BCP 14 <xref target="RFC2119" /> <xref target="RFC8174" /> when, and only when, they appear in all capitals, as shown here.</t>
</section>

<section anchor="the-open-public-client-oauth-profile"><name>The Open Public Client OAuth Profile</name>

<section anchor="overview"><name>Overview</name>
<t>OAuth 2 <xref target="RFC6749" /> can be used in many different ways. This document specifies one particular set of options to ensure interoperability and security. Servers may implement more options, but MUST support the flow as described in this document for interoperability with clients. Similarly, clients may choose to support additional flows but there is no guarantee that this will be interoperable.</t>
<t>The general flow works like this:</t>

<ol spacing="compact">
<li>The OAuth 2.0 Authorization Server Metadata <xref target="RFC8414" /> is fetched.</li>
<li>The client instance registers with the authorization server to get a client id using the OAuth 2.0 Dynamic Client Registration Protocol <xref target="RFC7591" />.</li>
<li>The client instance authorizes using the Authorization Code Grant flow (<xref target="RFC6749" />, Section 4.1) with PKCE <xref target="RFC7636" /> and Issuer Identification <xref target="RFC9207" />. The client instance also includes Resource Indicators <xref target="RFC8707" /> in the authorization request. Server support for Resource Indicators is RECOMMENDED but not required: an authorization server that does not support them ignores the unrecognised parameters (as required by <xref target="RFC6749" section="3.1" />) and the flow proceeds without them. The consequences of this are discussed in <xref target="authorization" /> and <xref target="security-considerations" />.</li>
<li>The client instance gets an access token and refresh token, as per <xref target="RFC6749" section="5" />.</li>
</ol>
<t>The access token can now be used as a Bearer token to authenticate requests to the application servers as per <xref target="RFC6750" /> for HTTP requests, or <xref target="RFC7628" /> for SASL authentication. When it expires, a new one can be requested using the refresh token as per <xref target="RFC6749" section="6"/>.</t>
<t>The rest of this document describes in detail each of the above steps.</t>
</section>

<section anchor="fetching-the-authorization-server-data"><name>Fetching the Authorization Server Metadata</name>
<t>The authorization server issuer identifier MUST be an HTTPS URL with no query parameters or fragment part. If a path is present, it MUST NOT contain <tt>.</tt> or <tt>..</tt> segments, and MUST NOT use percent-encoding for unreserved characters (as defined in <xref target="RFC3986" section="2.3" />). If the issuer identifier does not conform to these requirements, the flow MUST be aborted. Note that <tt>https://auth.example.com</tt> and <tt>https://auth.example.com/</tt> are both permitted, but they are distinct issuer identifiers: clients do not normalize one to the other, and the <tt>issuer</tt> metadata property (see below) must match exactly whichever form was used. The authorization server metadata may be fetched as specified in <xref target="RFC8414" section="3" /> by constructing a URL that places <tt>/.well-known/oauth-authorization-server</tt> after the host and before any path. For example, for an issuer identifier of <tt>https://auth.example.com/acmeinc</tt>, the authorization server metadata would be fetched from <tt>https://auth.example.com/.well-known/oauth-authorization-server/acmeinc</tt>.</t>
<t>If this request does not result in a successful response (a 200 OK HTTP status code with an "application/json" content type), the client MUST also try the OpenID Connect Discovery <xref target="OpenID.Discovery" /> metadata path. This is constructed by appending <tt>/.well-known/openid-configuration</tt> to the issuer identifier after stripping any trailing slash. For example, for an issuer identifier of <tt>https://auth.example.com/acmeinc</tt>, this would be <tt>https://auth.example.com/acmeinc/.well-known/openid-configuration</tt>. This fallback is necessary for compatibility with existing deployments that only serve metadata at the OpenID Connect path. See <xref target="security-considerations" /> for a discussion of the security implications of this fallback.</t>
<t>A successful response is indicated by a 200 OK HTTP status code. The response MUST have an "application/json" content type. If neither metadata path results in a successful response, the flow MUST be aborted. Clients MUST NOT follow HTTP redirects when fetching the metadata; a redirect MUST be treated the same as an error response.</t>
<t>The authorization server metadata is a JSON document with properties as specified in <xref target="RFC8414" />. It MUST include the following properties:</t>

<dl spacing="normal" newline="true">
<dt><tt>issuer</tt></dt>
<dd><t>The authorization server's issuer identifier. This MUST be identical to the issuer identifier from which this metadata URL was derived — not the URL from which it was fetched. For example, if the issuer identifier is <tt>https://auth.example.com/acmeinc</tt>, the issuer property MUST be <tt>https://auth.example.com/acmeinc</tt>, regardless of whether the metadata was fetched from <tt>https://auth.example.com/.well-known/oauth-authorization-server/acmeinc</tt> or <tt>https://auth.example.com/acmeinc/.well-known/openid-configuration</tt>. If not, the flow MUST be aborted.</t>
<t>This MUST be verified again during the authorization flow (see <xref target="authorization" />, or <xref target="RFC9207" /> for more details), to prevent against mix-up attacks.</t>
</dd>
<dt><tt>registration_endpoint</tt></dt>
<dd><t>The URL the client instance will use to register, to get a client id it needs for authorization (see <xref target="dynamic-client-registration" />).</t>
</dd>
<dt><tt>authorization_endpoint</tt></dt>
<dd><t>The URL the client instance will use to start the authorization process (see <xref target="authorization" />) once it has registered.</t>
</dd>
<dt><tt>token_endpoint</tt></dt>
<dd><t>The URL the client instance will use to get refresh and access tokens after successful authorization (see <xref target="obtaining-a-refresh-token" />).</t>
</dd>
<dt><tt>scopes_supported</tt></dt>
<dd><t>An array of supported scopes on the server (see <xref target="scopes" />).</t>
</dd>
<dt><tt>response_types_supported</tt></dt>
<dd><t>A list of response types supported in the OAuth authorization flow. This is an array of strings that MUST include "code".</t>
</dd>
<dt><tt>grant_types_supported</tt></dt>
<dd><t>A list of the OAuth 2.0 grant type values that this authorization server supports. This is an array of strings that MUST include "authorization_code" and "refresh_token".</t>
</dd>
<dt><tt>token_endpoint_auth_methods_supported</tt></dt>
<dd><t>This is an array of strings that MUST include "none".</t>
</dd>
<dt><tt>code_challenge_methods_supported</tt></dt>
<dd><t>An array of strings listing Proof Key for Code Exchange (PKCE) <xref target="RFC7636" />
code challenge methods supported by this authorization server.
This MUST include "S256".</t>
</dd>
<dt><tt>authorization_response_iss_parameter_supported</tt></dt>
<dd><t>This MUST have the boolean value true.</t>
</dd>
</dl>
<t>The metadata MAY include other properties, or other values for multi-valued properties, however clients are not required to understand or use any of them for interoperability.</t>
<t>It is RECOMMENDED servers support DPoP (<xref target="RFC9449" />) to allow sender-constrained refresh and access tokens for HTTP-based protocols. Servers that support DPoP MUST include the following property:</t>

<dl spacing="normal" newline="true">
<dt><tt>dpop_signing_alg_values_supported</tt></dt>
<dd>A JSON array containing a list of the JWS alg values (from the <xref target="IANA.JOSE.ALGS" /> registry) supported by the authorization server for DPoP proof JWTs, as defined in <xref target="RFC9449" />, Section 5.1.</dd>
</dl>
<t>Also of possible interest to client/server implementers following this document:</t>

<dl spacing="normal" newline="true">
<dt><tt>revocation_endpoint</tt></dt>
<dd>The URL the client instance can use to revoke their tokens, as per <xref target="RFC7009" />.</dd>
<dt><tt>revocation_endpoint_auth_methods_supported</tt></dt>
<dd>If a revocation_endpoint is included, this property MUST be included, and is an array of strings that MUST include "none".</dd>
</dl>
<t>Clients MUST verify the required properties are present and conform to the requirements of this document. If not, the server is not using OAuth in conformance with this document and no compatibility may be presumed. It is RECOMMENDED clients abort the flow in such a case.</t>
</section>

<section anchor="dynamic-client-registration">
<name>Dynamic Client Registration</name>

<t>To register, the client instance sends an HTTP POST to the client registration endpoint (as found in the metadata) with a content type of "application/json", and a body consisting of a JSON document with the following properties:</t>

<dl spacing="normal" newline="true">
<dt><tt>redirect_uris</tt></dt>
<dd><t>An array of URIs the client instance may use to receive back information at the end of the authorization flow. Each URI MUST satisfy all of these conditions:</t>

<ul spacing="compact">
<li><t>The URI MUST start with one of the following:</t>
<ul>
  <li><tt>http://127.0.0.1/</tt></li>
  <li><tt>http://[::1]/</tt></li>
  <li>A private-use scheme in reverse domain notation, e.g., <tt>com.example:/</tt>. Such a scheme MUST have at least one dot in it.</li>
</ul></li>
<li>The URI MUST NOT include two consecutive dots (e.g., <tt>/../</tt>).</li>
<li>The URI MUST NOT include a fragment part (<tt>#</tt>).</li>
</ul>
<t>The URI may include a path and query parameters.</t></dd>
<dt><tt>token_endpoint_auth_method</tt></dt>
<dd>This MUST be "none".</dd>
<dt><tt>grant_types</tt></dt>
<dd>This is an array of strings that MUST include "authorization_code" and "refresh_token".</dd>
<dt><tt>response_types</tt></dt>
<dd>This is an array of strings that MUST include "code".</dd>
<dt><tt>scope</tt></dt>
<dd><t>A string containing a space-separated list of scope values the client may request access for. (Note! This is not a JSON array.)</t>
<t>For compatibility with servers implementing OpenID Connect <xref target="OpenID.Core" />, if the server advertised the <tt>offline_access</tt> scope in the "scopes_supported" property of the authorization server metadata, the client instance MUST also register for this scope.</t></dd>
<dt><tt>client_name</tt></dt>
<dd>The name of the client software to be presented to the end-user during authorization.</dd>
<dt><tt>client_uri</tt></dt>
<dd>A URL for a web page providing information about the client software. This MUST use HTTPS.</dd>
<dt><tt>logo_uri</tt></dt>
<dd>A URL for a logo to display for this client software. This SHOULD be square, and in a PNG or SVG image format. This MUST use HTTPS.</dd>
<dt><tt>tos_uri</tt></dt>
<dd>A URL that points to a human-readable terms of service or license document for the client software. This MUST use HTTPS.</dd>
<dt><tt>policy_uri</tt></dt>
<dd>A URL that points to a human-readable privacy policy document for the client software. This MUST use HTTPS.</dd>
<dt><tt>software_id</tt></dt>
<dd>A unique identifier string (e.g., a Universally Unique Identifier
  (UUID)) assigned by the client developer or software publisher, used by
  registration endpoints to identify the client software doing the
  dynamic registration.  Unlike "client_id", which is issued by the
  authorization server and may vary between instances, the
  "software_id" SHOULD remain the same for all instances of the client
  software.  The "software_id" SHOULD remain the same across multiple
  updates or versions of the same piece of software. The value of this
  field is not intended to be human readable and is usually opaque to the
  client and authorization server.</dd>
<dt><tt>software_version</tt></dt>
<dd>A version identifier string for the client software identified by
  "software_id".  The value of the "software_version" SHOULD change on
  any update to the client software identified by the same
  "software_id".  The value of this field is intended to be compared
  using string equality matching and no other comparison semantics are
  defined by this specification.</dd>
</dl>
<t>If the server indicated in its metadata that it supports DPoP <xref target="RFC9449" /> and the client instance is intending to authenticate all requests using DPoP, the client instance SHOULD also include the following property:</t>

<dl spacing="normal" newline="true">
<dt><tt>dpop_bound_access_tokens</tt></dt>
<dd>true</dd>
</dl>
<t>(If set, the server MUST then require all token requests associated with the issued client id use DPoP. Note, DPoP is only currently defined for HTTP protocols, so this precludes usage for non-HTTP protocols as of time of writing.)</t>
<t>The server will check that all required properties are present and have valid values. Any unknown properties supplied by the client instance MUST just be ignored. The authorization server MAY replace any of the client's requested metadata values submitted during the registration and substitute them with suitable values.</t>
<t>If there is an exact match for all properties except for software_version, an existing registration may be returned. Otherwise, servers SHOULD create a new registration and client id.</t>
<t>There is no way to verify the authenticity of the information supplied by the client, however the general case of accurate information is still useful to the server, for example to be able to contact client authors to help debug issues if aberrant behaviour is observed. Servers MAY choose to ignore all of the information instead and just return a static client id to all requests.</t>
<t>The redirect URI restrictions MUST be enforced. These ensure the OAuth flow can only be completed by native clients — not web clients — as discussed in <xref target="native-clients-only" />. Since a malicious native client could present the user with a custom browser to phish credentials anyway, the lack of verification of client registration details does not provide additional danger beyond existing threats. Allowing seamless dynamic registration for web-based clients, however, unfortunately makes it much easier for a phishing site to gain access to an account, by sending the user through the OAuth flow.</t>
<t>If successful, the server responds with an HTTP 201 Created status code and a body of type "application/json", with the content being a JSON object containing all the properties submitted during registration (with their values as set by the server, if overwritten), plus the following property:</t>

<dl spacing="normal" newline="true">
<dt><tt>client_id</tt></dt>
<dd>The OAuth 2.0 client identifier string, used in the authorization flow (see <xref target="authorization" />).</dd>
</dl>
<t>Clients following this profile are public clients (see <xref target="RFC6749" section="2.1" />): they register with a <tt>token_endpoint_auth_method</tt> of "none" and authenticate to the token endpoint using only PKCE. The server MUST NOT issue a client secret in the registration response, so the <tt>client_secret</tt> and <tt>client_secret_expires_at</tt> properties defined in <xref target="RFC7591" section="3.2.1" /> MUST NOT be present. A native client cannot keep a secret confidential, so a secret would offer no security benefit, and a client that expected to use one would not be interoperable with servers that correctly omit it. If a server nonetheless returns a <tt>client_secret</tt>, the client instance MUST ignore it and continue as a public client.</t>
<t>If the registration fails, the server will respond with an HTTP 400 status code and a JSON body as described in <xref target="RFC7591" />, section 3.2.2.</t>
</section>

<section anchor="authorization"><name>Authorization</name>
<t>Clients construct an authorization request URL by taking the <tt>authorization_endpoint</tt> from the authorization server metadata and adding the following additional query parameters:</t>

<dl spacing="normal" newline="true">
<dt><tt>client_id</tt></dt>
<dd>The client id as returned in the registration.</dd>
<dt><tt>redirect_uri</tt></dt>
<dd><t>One of the redirect uris registered by the client instance. This MUST be identical to the registered URI. The one exception to this is if a URI with the prefix "http://127.0.0.1/" or "http://[::1]/" was registered, in which case the matching URI MUST also include an arbitrary port.</t>
<t>For example, if <tt>http://127.0.0.1/redirect</tt> was registered, then the client instance could send <tt>http://127.0.0.1:49152/redirect</tt> as the redirect_uri for authorization.</t></dd>
<dt><tt>response_type</tt></dt>
<dd>This MUST be "code".</dd>
<dt><tt>scope</tt></dt>
<dd><t>A space delimited set of scopes the client instance would like access to. This MUST be a subset of the scopes registered for this client id.</t>
<t>For compatibility with servers implementing OpenID Connect <xref target="OpenID.Core" />, if the server advertised the <tt>offline_access</tt> scope in the "scopes_supported" property of the authorization server metadata, the client instance MUST also request this scope.</t></dd>
<dt><tt>code_challenge</tt></dt>
<dd><t>A PKCE code challenge as per <xref target="RFC7636" />, using SHA 256. To generate a challenge, first generate a code_verifier: a high-entropy cryptographic random string using the unreserved characters <tt>[A-Z] / [a-z] / [0-9] / "-" / "." / "_" / "~"</tt> from Section 2.3 of <xref target="RFC3986" />, with a minimum length of 43 characters and a maximum length of 128 characters.</t>
<t>The code_challenge is then <tt>BASE64URL-ENCODE(SHA256(ASCII(code_verifier)))</tt>.</t></dd>
<dt><tt>code_challenge_method</tt></dt>
<dd>This MUST be "S256".</dd>
<dt><tt>resource</tt></dt>
<dd>The URL for the initial resource endpoint (as returned in the autodiscover) you wish to access after successful authorization. For example, <tt>https://api.example.com/jmap/session</tt>. If you wish to use multiple protocols, the client instance MUST include multiple "resource" query parameters: one for each endpoint. Server support for Resource Indicators <xref target="RFC8707" /> is RECOMMENDED; servers that do not support them will ignore these parameters and the flow will still succeed. Servers that do support Resource Indicators MUST verify that all requested endpoints are permitted locations to send access tokens to (i.e., they are the real resource endpoints associated with this authorization server).</dd>
<dt><tt>state</tt></dt>
<dd>An opaque value used by the client instance to verify that an authorization response is due to a request that the client instance initiated. The authorization server will include this value when redirecting the user-agent back to the client instance.  Client instances MUST generate a state with a unique, unguessable random string when initiating an authorization request.</dd>
<dt><tt>login_hint (optional)</tt></dt>
<dd>The username the user originally asked to log in with. The server can prefill this in a login form. Note, the user may choose to log in with a different username.</dd>
</dl>

<t>After constructing the authorization request URL, the client instance MUST use platform-specific APIs to open it in an external user-agent. It MUST NOT use an embedded user-agent (such as a web view) within the application, as required by <xref target="RFC8252" section="8.12" />. Typically, the external user-agent used is the default browser, that is, the application configured for handling "http" and "https" scheme URIs on the system. See <xref target="RFC8252" section="6"/> for a discussion of best practices and alternatives, such as in-app browser tabs, which are external user-agents that render within the application while retaining the security properties of the browser. The user-agent MUST support session cookies, JavaScript, and the Web Authentication API <xref target="WebAuthn" />.</t>

<t>Embedded user-agents are prohibited because they undermine much of the security benefit of this profile. An embedded user-agent is under the control of the client application, which can therefore observe and modify everything the user enters, including their password and any second factor, and the user has no way to verify which site they are actually entering their credentials into. An external user-agent, by contrast, shares the user's existing authentication state and credential stores with the rest of the system. This allows single sign-on, password managers, and origin-bound authenticators such as passkeys <xref target="WebAuthn" /> to work, and allows the browser's built-in phishing protections to apply. Users are also better able to recognise and reason about a login page shown in their familiar browser than one shown inside an arbitrary application.</t>

<t>This restriction is a requirement on clients and is not reliably enforceable by the authorization server, as an embedded user-agent may be indistinguishable from an external one. Servers MAY use heuristics (for example, the <tt>User-Agent</tt> header field) to detect and refuse authorization from known embedded user-agents, as suggested in <xref target="RFC8252" section="8.12" />, but the security of this profile does not depend on them doing so. As discussed in <xref target="security-considerations" />, a malicious native application could already present the user with a fake browser, so this profile does not attempt to defend against a malicious client; the requirement ensures that well-behaved clients provide the security properties users expect.</t>

<t>The authorization server MUST verify that all of the parameters defined above are present and conform to the restrictions in this document. A request in which any parameter is included more than once MUST be rejected, with the exception of the <tt>resource</tt> parameter, which may be repeated as described above.</t>

<t>The client instance MAY include additional request parameters defined by other OAuth extensions, for example the <tt>prompt</tt> or <tt>ui_locales</tt> parameters defined by OpenID Connect Core <xref target="OpenID.Core" />. This does not permit alternative or additional values for the parameters defined above; in particular, it does not relax the <tt>redirect_uri</tt> matching rules, and a request whose defined parameters do not conform MUST be rejected regardless of any other parameters present. The authorization server MUST ignore any request parameter it does not recognise, as required by <xref target="RFC6749" section="3.1" />, and clients MUST NOT depend on such parameters being understood for interoperability. Ignoring unrecognised parameters is safe because they cannot affect the processing of the parameters defined by this document, and it allows this profile to coexist with other OAuth extensions that both the client and server happen to support.</t>

<t>If verified, the authorization server will authenticate the user and ask them if they wish to grant authorization to the client.</t>

<t> If the request fails due to a missing, invalid, or mismatching redirection URI, or if the client identifier is missing or invalid, the authorization server SHOULD inform the user of the error and MUST NOT automatically redirect the user-agent to the invalid redirection URI. If the authorization request fails for any other reason, the client instance will receive an error response via the redirect_uri. This MUST include an error query parameter with an appropriate error code, as defined in <xref target="RFC6749" section="4.1.2.1"/>.</t>

<t>If authorization is successful, the client instance will receive a response via the redirect_uri, which will include the following query parameters:</t>

<dl spacing="normal" newline="true">
<dt><tt>code</tt></dt>
<dd>The authorization code. This may be exchanged for the refresh token. This code MUST expire, and MUST remain valid for at least 10 minutes from authorization. It MUST NOT be used again once the client instance has successfully exchanged it for a refresh token. Doing so may cause the server to detect it as stolen and revoke all associated tokens.</dd>
<dt><tt>state</tt></dt>
<dd>The value of the state parameter that was passed in with the initial request.</dd>
<dt><tt>iss</tt></dt>
<dd>The issuer identifier of the authorization server.</dd>
</dl>
<t>The client instance MUST verify all of the following:</t>

<ul spacing="compact">
<li>The "iss" returned is identical to the "issuer" property in the authorization server metadata. This is critical for preventing against various mix-up attacks should a malicious OAuth metadata object be fetched, as discussed in the security considerations.</li>
<li>The state returned matches exactly the state it sent, to verify that this
request was indeed initiated by the client and not an attacker.</li>
</ul>
<t>If any verification fails, the client instance MUST abort the flow and not send the authorization code anywhere. It MUST return a page to the user-agent with a description of what went wrong and what the user can do next to solve the problem.</t>
</section>

<section anchor="obtaining-a-refresh-token"><name>Obtaining a Refresh Token</name>
<t>Following authorization, the client instance will obtain initial refresh and
access tokens by making a POST request to the token_endpoint URL. The following parameters MUST be present, using the "application/x-www-form-urlencoded" format with a character encoding of UTF-8 in the HTTP request entity-body:</t>

<dl spacing="normal" newline="true">
<dt><tt>client_id</tt></dt>
<dd>The client id as returned in the registration.</dd>
<dt><tt>redirect_uri</tt></dt>
<dd>The redirect_uri parameter sent with the authorization request from which the code was obtained.</dd>
<dt><tt>grant_type</tt></dt>
<dd>This MUST be "authorization_code".</dd>
<dt><tt>code</tt></dt>
<dd>The code returned via the redirect back from authorization.</dd>
<dt><tt>code_verifier</tt></dt>
<dd>The code_verifier generated for the authorization (the random string generated in the authorization step, as per <xref target="RFC7636" />).</dd>
</dl>
<t>The client instance MAY include additional parameters defined by other OAuth extensions. The server MUST ignore any parameter it does not recognise, as required by <xref target="RFC6749" section="3.2" />, and clients MUST NOT depend on such parameters being understood for interoperability. If using DPoP, the client instance MUST also set a DPoP header in accordance with <xref target="RFC9449" section="5" />.</t>
<t>The server will verify the parameters and if successful, return a <tt>200 OK</tt> response with a content type of application/json. The body will be a JSON
object with the following properties:</t>

<dl spacing="normal" newline="true">
<dt><tt>access_token</tt></dt>
<dd>A bearer token used to authenticate API requests. This will be valid for a fixed, limited time.</dd>
<dt><tt>token_type</tt></dt>
<dd>The type of the access token. This MUST be "Bearer", or "DPoP" if the client is using DPoP (<xref target="RFC9449" />).</dd>
<dt><tt>expires_in</tt></dt>
<dd>The lifetime in seconds of the access token. For example, the value <tt>3600</tt> denotes that the access token will expire in one hour from the time the response was generated.</dd>
<dt><tt>scope</tt></dt>
<dd><t>The space delimited set of scopes that this access token may use.</t>
<t>Note, this MAY be different to the set of scopes requested. Servers MAY allow users to choose to authorize only a subset of the requested scopes. Clients MUST check the set of scopes granted is sufficient for its needs. The server MAY also return scopes beyond those requested, for example a server that adds JMAP support when previously it only supported IMAP may choose to give scopes that enable JMAP access to the same data, even if only the IMAP scope was requested, so the client instance can seamlessly upgrade protocols.</t></dd>
<dt><tt>refresh_token</tt></dt>
<dd>The refresh token to use next time the client instance needs to get a new access token.</dd>
</dl>

<t>If the request fails, the server MUST return a <tt>400 Bad Request</tt> response with a content type of application/json. The body will be a JSON object with properties as described in <xref target="RFC6749" section="5.2"/>.</t>
</section>

<section anchor="using-the-access-token"><name>Using the Access Token</name>
<t>The client instance is now authenticated. It can connect to the servers given in the discovered autoconfiguration with the Bearer scheme <xref target="RFC6750" />. For HTTP-based protocols, this means setting an <tt>Authorization</tt> header with the value <tt>Bearer {access_token}</tt> (where <tt>{access_token}</tt> is replaced with the value of the access_token). If using DPoP, the client instance must also set a DPoP header in accordance with <xref target="RFC9449" section="7"/>.</t>
<t>For protocols that use SASL authentication, such as IMAP, POP, and SMTP, the access token is used in accordance with the OAUTHBEARER mechanism defined in <xref target="RFC7628" />. Servers MUST NOT require the username associated with the resource (the "authzid") be included in the GS2 header.</t>
</section>

<section anchor="scopes"><name>Scopes</name>
<t>To work interoperably, clients and servers must use a common set of scopes for access. This document creates a new IANA registry for such scopes (see <xref target="scopes-registry" />), and registers scopes to cover IMAP, SMTP, POP, CardDAV, CalDAV, and JMAP.</t>
<t>Registration of a scope in that registry does not by itself impose any requirement on OAuth implementations in general (see <xref target="scopes-registry" />). This profile, however, does require support: a server implementing this profile MUST support each of the scopes registered by this document (<xref target="initial-registry-contents" />) whose definition covers a protocol or type of data that the server offers. For example, a server that offers IMAP and SMTP MUST support <tt>urn:ietf:params:oauth:scope:mail</tt>, and a server that offers CalDAV MUST support <tt>urn:ietf:params:oauth:scope:calendars</tt>. Supporting a scope means advertising it in the <tt>scopes_supported</tt> metadata property, accepting it in registration and authorization requests, and granting the access described in its registration when it is authorized.</t>
<t>Servers MAY additionally support other scopes, for example finer-grained or vendor-specific scopes, but clients following this profile can only rely on the registered scopes being available.</t>
</section>

<section anchor="getting-a-new-access-token"><name>Getting a New Access Token</name>
<t>Clients SHOULD keep using an access token they have been issued until it expires,
which will result in getting a <tt>401</tt> error back.</t>
<t>When the access token expires, the client instance MUST get a new one by making another POST request to the authorization server token endpoint. The following parameters MUST be present, using the "application/x-www-form-urlencoded" format with a character encoding of UTF-8 in the HTTP request entity-body:</t>

<dl spacing="normal" newline="true">
<dt><tt>client_id</tt></dt>
<dd>The client id as returned in the registration.</dd>
<dt><tt>grant_type</tt></dt>
<dd>This MUST be "refresh_token".</dd>
<dt><tt>refresh_token</tt></dt>
<dd>The refresh token returned last time the client instance obtained a new access token.</dd>
</dl>
<t>The success and failures responses are identical to those documented in
"Obtaining a refresh token" (<xref target="obtaining-a-refresh-token" />).</t>
<t>A new refresh token SHOULD be returned in the response and the client instance MUST replace their previous refresh token with this if given. The client instance MUST NOT try to use an old refresh token again; this SHOULD result in the authorization being revoked as a protection against leaked refresh tokens.</t>
<t>If a user has multiple devices with the same client software installed, each instance of the client MUST obtain separate authorization. A refresh token MUST NOT be shared between devices.</t>
</section>

<section anchor="token-expiry-times"><name>Token Expiry Times</name>
<t>To ensure a good user experience, access tokens MUST have a lifetime of at least 1 hour. Refresh tokens SHOULD NOT expire, unless the user explicitly revokes the token or it has been unused for a considerable period of time (at least 30 days).</t>
<t>For stateful connection-based protocols such as SMTP and IMAP the access token is only presented on first connection. Expiry of the access token SHOULD NOT affect current IMAP/SMTP etc. sessions that have already authenticated. However, should the server choose to force reauthorization it MUST do so by unilaterally closing the connection, as there is no way to reauthenticate an existing session in these protocols and all clients have to be able to handle reconnection in the case of dropped connections.</t>
<t>When the server revokes a refresh token (for example, because the user has revoked the client's authorization, or because the token has been detected as leaked), it MUST close any existing stateful connections that were authenticated using access tokens issued under that refresh token.</t>
<t>A client instance cannot detect that its refresh token has been revoked until it next attempts to use it. Once a client instance does detect revocation (by receiving an <tt>invalid_grant</tt> error from the token endpoint), it MUST discard the refresh token and any associated access tokens, MUST close any stateful connections it still has open that were authenticated with them, and MUST obtain new authorization before reconnecting (see <xref target="dynamic-client-registration-considerations" />).</t>
</section>

<section anchor="dynamic-client-registration-considerations">
<name>Client Id Validity</name>
<t>The client id MUST remain valid for as long as there is a valid refresh token associated with that client id.</t>
<t>To ensure a good user experience, the client id assigned upon dynamic registration SHOULD be valid for use in the authorization flow (<xref target="authorization"/>) for at least one hour after issuance.</t>
<t>Clients MUST associate a client id with each refresh token it has. If the refresh token is invalidated and the client instance has to reauthenticate, it MUST submit the dynamic client registration (<xref target="dynamic-client-registration" />) again and use the client id returned, which MAY be different than the previous client id.</t>
<t>Servers that wish to store client registration information MUST be careful of resource exhaustion attacks. A RECOMMENDED approach is to:</t>
<ul>
  <li>Normalize and securely hash the submitted registration data.</li>
  <li>Check if this hash already exists in the database, and if so return the existing client id issued for this registration.</li>
  <li>Otherwise, keep the registration in temporary storage and only insert it into the database upon successful authorization. Creating a new client id in the temporary storage can be rate limited, for example by IP.</li>
  <li>Remove any client registrations from the database whenever there are no valid refresh tokens associated with that client id.</li>
</ul>
</section>
</section>

<section anchor="security-considerations"><name>Security Considerations</name>
<t>This profile mandates best practices for OAuth with native clients, as defined in <xref target="RFC8252" />.  A thorough discussion of the security considerations generally applicable to OAuth is out of scope for this document, but can be found in <xref target="RFC6819" /> and in the OAuth 2.0 Security Best Current Practice <xref target="RFC9700" /> (BCP 240), as well as the security considerations (Section 10) of <xref target="RFC6749" />.</t>
<t>Implementers are encouraged to read all of the above documents for a more thorough consideration of the specific threats and mitigations with OAuth.</t>
<t>The choices made for this profile are intended to mitigate as far as possible the inherent risks that come from allowing arbitrary clients to talk to arbitrary servers.</t>
<t>The key restriction of this profile is that the redirect_uri MUST be something only a native client can access. If the user has downloaded and run a malicious native app, it could already undetectably spoof the user's browser to phish them, or in unsandboxed environments install malware, so supporting the OAuth flow from an unknown client is not increasing risks. Indeed, it is more secure than the current alternative, which is legitimate clients storing the user's password.</t>
<t>Access tokens, refresh tokens, and authorization codes are bearer credentials: anyone who obtains one can use it. Clients MUST only send them over connections secured with TLS, and MUST verify the server's certificate as described in <xref target="RFC6125" />. This applies to the token endpoint, to HTTP-based resource servers as required by <xref target="RFC6750" section="5.2" />, and to protocols using SASL: for IMAP, POP, and SMTP, the client MUST negotiate TLS (either Implicit TLS or STARTTLS, see <xref target="RFC8314" />) before sending the OAUTHBEARER authentication exchange. Servers MUST NOT accept access tokens over connections that are not protected by TLS. The one exception is the redirect back to a loopback redirect URI (<xref target="dynamic-client-registration" />), which uses the "http" scheme. This carries only the authorization code, and the connection never leaves the user's device. The code is additionally protected by PKCE, so it is useless to anyone other than the client instance that holds the corresponding code verifier.</t>
<t>The dynamic registration part of this document is not a security component, as there is no way to verify any of the data. The data in the registration may be shown to the user as part of the authorization flow, which may help with phishing, but as noted above the redirect_uri can only be used by a native app, which could already phish the user. However, the registration gives the server more information to detect suspicious behaviour, which can help it to detect compromised users and devices more easily.</t>
<t>The issuer is expected to be autodetected from the user's email address. A threat scenario that must be considered is the user making a small typo in the domain (especially for a common email service), and an attacker controlling this domain. In this scenario the client instance will fetch the OAuth metadata from the attacker's server, and has no way to know it is not the real server the user wishes to connect to. This leads to a number of threats:</t>

<ol>
<li><t>The attacker defines a malicious authorization_endpoint under their control. They attempt to phish the user's credentials with this. This is not something that can be specifically mitigated by the requirements of this document, however requiring origin-bound authentication such as passkeys <xref target="WebAuthn" /> for authentication will mitigate this, and most browsers have a block list of known phishing sites that can also help mitigate this.</t>
</li>
<li><t>The attacker uses the real <tt>authorization_endpoint</tt> and <tt>token_endpoint</tt> values in its metadata, but uses their own resource servers. This is protected against via the issuer identifier check. The authorization response will include an "iss" parameter which will be the legitimate issuer identifier. However, the issuer for the metadata will not match, as this must be at the attacker's domain (if it were not, the client instance will have aborted the flow after fetching the metadata, as it would not match the domain it was fetched from). Therefore a client instance following this specification will abort the flow and not send the authorization code to the token endpoint.</t>
<t>Servers that support Resource Indicators (<xref target="RFC8707" />) have an additional layer of protection here — the client instance sends the list of all resource endpoints it intends to connect to with the authorization request, and if an unknown resource server is present, the server can reject the request. Support for Resource Indicators is RECOMMENDED in this profile; the issuer identifier check is the primary defence against this attack. However, servers are encouraged to support Resource Indicators as defence-in-depth.</t></li>
<li><t>The attacker uses the real <tt>authorization_endpoint</tt> value and the real resource servers in its metadata, but uses their own <tt>token_endpoint</tt>. The issuer identifier check above will also protect against this.</t></li>
</ol>
<t>This specification allows clients to fall back to the OpenID Connect Discovery metadata path if the RFC 8414 metadata path fails. Implementers should be aware that these two paths have different security properties when the issuer identifier contains a path component. The RFC 8414 path inserts the well-known component at the host root (e.g., <tt>https://auth.example.com/.well-known/oauth-authorization-server/acmeinc</tt>), meaning only an entity with control over the host can serve this document. The OpenID Connect Discovery path appends the well-known component to the issuer (e.g., <tt>https://auth.example.com/acmeinc/.well-known/openid-configuration</tt>), meaning any entity that controls the path can serve this document. In multi-tenant environments where different entities control different paths on the same host, the OpenID Connect Discovery fallback allows a tenant to serve their own metadata without host-level control. The issuer identifier verification remains the primary defence against mix-up attacks in all cases, but operators of shared hosts should be aware that the RFC 8414 path provides an additional layer of host-level vetting that the OpenID Connect Discovery path does not.</t>
</section>

<section anchor="iana-considerations">
<name>IANA Considerations</name>

<section anchor="scopes-registry">
<name>Interoperable OAuth Scopes Registry</name>

<t>IANA shall add a new registry called "Interoperable OAuth Scopes" to the "OAuth Parameters" registry group <xref target="IANA.OAuth.Parameters" />. The registry records OAuth scope values that have a standardised, interoperable meaning, such that a client and a server with no pre-existing relationship can agree on the access a scope grants. Each scope provides specific access rights to protected resources, and the registry records a reference to the specification that defines those rights.</t>

<section anchor="registry-applicability">
<name>Applicability of Registered Scopes</name>

<t>Registration of a scope in this registry does not, by itself, impose any requirement on OAuth authorization servers, resource servers, or clients in general. An OAuth implementation is not required to recognise, support, or grant any scope in this registry merely because it is registered. In this respect the registry is a coordination mechanism, ensuring a given scope value has a single well-defined meaning wherever it is used, rather than a statement of what OAuth implementations must support.</t>

<t>Support for a registered scope is required only where a profile or other specification that references this registry mandates it. Such a specification MUST clearly identify which implementations are required to support which scopes. This document is one such profile: the scopes that servers implementing it MUST support are specified in <xref target="scopes" />. Other profiles MAY reference scopes in this registry and define their own support requirements, and the same scope MAY be required by more than one profile.</t>
</section>

<section anchor="scope-naming">
<name>Scope Naming Conventions</name>

<t>Scope names in this registry are case sensitive. Names MUST NOT match other registered names in a case-insensitive manner unless the designated experts state that there is a compelling reason to allow an exception.</t>

<t>A newly defined scope registered in this registry MUST be a URN in the <tt>urn:ietf:params:oauth:scope:</tt> sub-namespace of the OAuth URN namespace <xref target="RFC6755" />, of the form <tt>urn:ietf:params:oauth:scope:&lt;name&gt;</tt>, for example <tt>urn:ietf:params:oauth:scope:mail</tt>. The <tt>&lt;name&gt;</tt> component SHOULD be a short, lowercase, descriptive identifier for the class of data or resources to which the scope grants access. Using this namespace guarantees the value cannot collide with scope strings defined elsewhere, and makes clear to implementers that the value has a standardised meaning. Any such URN MUST also be registered in the IANA "OAuth URI" registry <xref target="IANA.OAuth.Parameters" /> established by <xref target="RFC6755" />, referencing the same specification document(s).</t>

<t>An unstructured scope string, that is, one that is not a URN in the above sub-namespace, MAY be registered only where all of the following apply:</t>
<ul>
<li>The scope value is already defined by an existing, publicly available and stable specification outside of the IETF's control, or is already in widespread deployment with a single well-understood meaning.</li>
<li>The purpose of the registration is to allow profiles referencing this registry to interoperate with that existing usage. Assigning a new URN for the same purpose would therefore harm interoperability rather than help it.</li>
<li>The value does not conflict with any existing registration in this registry.</li>
</ul>
<t>Unstructured strings MUST NOT be registered for newly defined scopes. Where a specification wishes to register a scope with an unstructured name, the designated experts MUST verify that the referenced specification defines the value with the meaning given in the registration request.</t>
</section>

<section anchor="registration-procedure">
<name>Registration Procedure</name>

<t>Values are registered via Specification Required <xref target="RFC8126" />. Registration requests should be sent to the <eref target="mailto:oauth-ext-review@ietf.org">oauth-ext-review@ietf.org</eref> mailing list to initiate a two-week review period. However, to allow for the allocation of values prior to publication of the final version of a specification, the designated experts may approve registration once they are satisfied that the specification will be completed and published. However, if the specification is not completed and published in a timely manner, as determined by the designated experts, the designated experts may request that IANA withdraw the registration.</t>

<t>Registration requests sent to the mailing list for review should use an appropriate subject (e.g., "Request to register Interoperable OAuth Scope: example").</t>

<t>Within the review period, the designated experts will either approve or deny the registration request, communicating this decision to the review list and IANA. Denials should include an explanation and, if applicable, suggestions as to how to make the request successful. If the designated experts are not responsive, the registration requesters should contact IANA to escalate the process.</t>

<t>Designated experts should apply the following criteria when reviewing proposed registrations:</t>
<ul>
<li>The proposed scope must be unique, that is, it must not duplicate the functionality of an existing registration.</li>
<li>The proposed scope is likely to be generally applicable, that is, useful to multiple independent clients and servers, as opposed to being used for a single application or deployment.</li>
<li>The proposed scope name follows the naming conventions in <xref target="scope-naming" />. In particular, an unstructured (non-URN) scope name is only permitted under the conditions given there.</li>
<li>The description clearly defines the access rights granted by the scope, with reference to publicly available and stable documentation sufficient for interoperability.</li>
<li>The specification document(s) identify which implementations are required to support the scope (for example, by defining or referencing a profile that mandates it), since registration alone imposes no such requirement (see <xref target="registry-applicability" />).</li>
<li>The change controller is clearly identified.</li>
</ul>

<t>IANA must only accept registry updates from the designated experts and should direct all requests for registration to the review mailing list.</t>

<t>In order to enable broadly informed review of registration decisions, there should be multiple designated experts to represent the perspectives of different applications using this specification. In cases where registration may be perceived as a conflict of interest for a particular expert, that expert should defer to the judgment of the other experts.</t>

<t>The mailing list is used to enable public review of registration requests, which enables both designated experts and other interested parties to provide feedback on proposed registrations. Designated experts may allocate values prior to publication of the final specification. This allows authors to receive guidance from the designated experts early, so any identified issues can be fixed before the final specification is published.</t>

</section>

<section anchor="registration-template">
<name>Registration Template</name>

<dl newline="false" spacing="compact">
<dt>Scope Name:</dt>
    <dd>The OAuth scope value requested (e.g., <tt>urn:ietf:params:oauth:scope:mail</tt>). This name is case sensitive and MUST conform to <xref target="scope-naming" />.</dd>

<dt>Description:</dt>
    <dd>A clear description of what access rights this scope grants to the client, with reference to publicly available and stable documentation sufficient for interoperability.</dd>

<dt>Change Controller:</dt>
    <dd>For IETF Stream RFCs, list "IETF". For others, give the name of the responsible party. Other details (e.g., postal address, email address, home page URI) may also be included.</dd>

<dt>Specification Document(s):</dt>
    <dd>Reference to the document or documents that specify the scope, preferably including URIs that can be used to retrieve copies of the documents. An indication of the relevant sections may also be included but is not required. The referenced document(s) MUST identify which implementations are required to support the scope (see <xref target="registry-applicability" />).</dd>
</dl>

</section>

<section anchor="initial-registry-contents">
<name>Initial Registry Contents</name>

<dl newline="false" spacing="compact">
<dt>Scope Name:</dt> <dd><tt>urn:ietf:params:oauth:scope:mail</tt></dd>
<dt>Description:</dt>
<dd>
  <t>Requests access for a client to manage the user's email. This MUST include full access via all of the following protocols that the server supports:</t>
  <ul>
    <li>IMAP <xref target="RFC9051" /></li>
    <li>POP <xref target="RFC1939" /></li>
    <li>SMTP submission <xref target="RFC6409" /></li>
    <li>JMAP <xref target="RFC8620" /> - this scope grants the right to use all JMAP capabilities that the server supports for which <tt>urn:ietf:params:oauth:scope:mail</tt> is included in the Interoperable OAuth Scopes column of the JMAP capabilities registry.</li>
  </ul>
</dd>
<dt>Change Controller:</dt> <dd>IETF</dd>
<dt>Specification Document(s):</dt> <dd><xref target="initial-registry-contents" /> and <xref target="scopes" /> of this document</dd>
</dl>

<dl newline="false" spacing="compact">
<dt>Scope Name:</dt> <dd><tt>urn:ietf:params:oauth:scope:contacts</tt></dd>
<dt>Description:</dt> <dd>Requests access for a client to manage the user's contacts. This MUST include full access via the CardDAV protocol <xref target="RFC6352" /> if the server supports it. If the server supports JMAP <xref target="RFC8620" />, this scope grants the right to use all JMAP capabilities that the server supports for which <tt>urn:ietf:params:oauth:scope:contacts</tt> is included in the Interoperable OAuth Scopes column of the JMAP capabilities registry.</dd>
<dt>Change Controller:</dt> <dd>IETF</dd>
<dt>Specification Document(s):</dt> <dd><xref target="initial-registry-contents" /> and <xref target="scopes" /> of this document</dd>
</dl>

<dl newline="false" spacing="compact">
<dt>Scope Name:</dt> <dd><tt>urn:ietf:params:oauth:scope:calendars</tt></dd>
<dt>Description:</dt> <dd>Requests access for a client to manage the user's calendars. This MUST include full access via the CalDAV protocol <xref target="RFC4791" /> if the server supports it. If the server supports JMAP <xref target="RFC8620" />, this scope grants the right to use all JMAP capabilities that the server supports for which <tt>urn:ietf:params:oauth:scope:calendars</tt> is included in the Interoperable OAuth Scopes column of the JMAP capabilities registry.</dd>
<dt>Change Controller:</dt> <dd>IETF</dd>
<dt>Specification Document(s):</dt> <dd><xref target="initial-registry-contents" /> and <xref target="scopes" /> of this document</dd>
</dl>

</section>
</section>

<section anchor="oauth-uri-registrations">
<name>OAuth URI Registrations</name>

<t>This section registers the following values in the IANA "OAuth URI" registry <xref target="IANA.OAuth.Parameters" /> established by <xref target="RFC6755" />. They are the scope values defined by this document within the <tt>urn:ietf:params:oauth:scope:</tt> sub-namespace, as required by <xref target="scope-naming" />.</t>

<dl newline="false" spacing="compact">
<dt>URN:</dt> <dd><tt>urn:ietf:params:oauth:scope:mail</tt></dd>
<dt>Common Name:</dt> <dd>Interoperable OAuth scope granting access to the user's email</dd>
<dt>Change Controller:</dt> <dd>IETF</dd>
<dt>Specification Document(s):</dt> <dd><xref target="initial-registry-contents" /> of this document</dd>
</dl>

<dl newline="false" spacing="compact">
<dt>URN:</dt> <dd><tt>urn:ietf:params:oauth:scope:contacts</tt></dd>
<dt>Common Name:</dt> <dd>Interoperable OAuth scope granting access to the user's contacts</dd>
<dt>Change Controller:</dt> <dd>IETF</dd>
<dt>Specification Document(s):</dt> <dd><xref target="initial-registry-contents" /> of this document</dd>
</dl>

<dl newline="false" spacing="compact">
<dt>URN:</dt> <dd><tt>urn:ietf:params:oauth:scope:calendars</tt></dd>
<dt>Common Name:</dt> <dd>Interoperable OAuth scope granting access to the user's calendars</dd>
<dt>Change Controller:</dt> <dd>IETF</dd>
<dt>Specification Document(s):</dt> <dd><xref target="initial-registry-contents" /> of this document</dd>
</dl>
</section>
<section>
<name>Update to the JMAP Capabilties Registry</name>

<t>IANA shall update the "JMAP Capabilities" registry established in <xref target="RFC8620" /> to add a new column called "Interoperable OAuth Scopes". For each capability, this column shall contain a list of zero or more scopes registered in the Interoperable OAuth Scopes Registry. Authorization for any scope in the list will grant access to use that JMAP capability, if the capability is supported by the server.</t>

<t>Future registrations in the "JMAP Capabilities" registry SHOULD specify a value for the Interoperable OAuth Scopes column in addition to the fields established in the registration template by <xref target="RFC8620" section="9.4.5"/>. If omitted, registrations will be added with no value for this column (i.e., no interoperable scopes are available to grant access to this capability via OAuth).</t>

<section>
<name>Initial Interoperable OAuth Scopes Values for the JMAP Capabilities Registry</name>

<t>IANA shall update the existing JMAP Capabilities registrations with the following values for the Interoperable OAuth Scopes column.</t>

<dl newline="false" spacing="compact">
<dt>Capability Name:</dt> <dd>urn:ietf:params:jmap:core</dd>
<dt>Interoperable OAuth Scopes:</dt> <dd>urn:ietf:params:oauth:scope:mail, urn:ietf:params:oauth:scope:contacts, urn:ietf:params:oauth:scope:calendars</dd>
</dl>

<dl newline="false" spacing="compact">
<dt>Capability Name:</dt> <dd>urn:ietf:params:jmap:mail</dd>
<dt>Interoperable OAuth Scopes:</dt> <dd>urn:ietf:params:oauth:scope:mail</dd>
</dl>

<dl newline="false" spacing="compact">
<dt>Capability Name:</dt> <dd>urn:ietf:params:jmap:mdn</dd>
<dt>Interoperable OAuth Scopes:</dt> <dd>urn:ietf:params:oauth:scope:mail</dd>
</dl>

<dl newline="false" spacing="compact">
<dt>Capability Name:</dt> <dd>urn:ietf:params:jmap:smimeverify</dd>
<dt>Interoperable OAuth Scopes:</dt> <dd>urn:ietf:params:oauth:scope:mail</dd>
</dl>

<dl newline="false" spacing="compact">
<dt>Capability Name:</dt> <dd>urn:ietf:params:jmap:submission</dd>
<dt>Interoperable OAuth Scopes:</dt> <dd>urn:ietf:params:oauth:scope:mail</dd>
</dl>

<dl newline="false" spacing="compact">
<dt>Capability Name:</dt> <dd>urn:ietf:params:jmap:vacationresponse</dd>
<dt>Interoperable OAuth Scopes:</dt> <dd>urn:ietf:params:oauth:scope:mail</dd>
</dl>

<dl newline="false" spacing="compact">
<dt>Capability Name:</dt> <dd>urn:ietf:params:jmap:blob</dd>
<dt>Interoperable OAuth Scopes:</dt> <dd>urn:ietf:params:oauth:scope:mail, urn:ietf:params:oauth:scope:contacts, urn:ietf:params:oauth:scope:calendars</dd>
</dl>

<dl newline="false" spacing="compact">
<dt>Capability Name:</dt> <dd>urn:ietf:params:jmap:quota</dd>
<dt>Interoperable OAuth Scopes:</dt> <dd>urn:ietf:params:oauth:scope:mail, urn:ietf:params:oauth:scope:contacts, urn:ietf:params:oauth:scope:calendars</dd>
</dl>

<dl newline="false" spacing="compact">
<dt>Capability Name:</dt> <dd>urn:ietf:params:jmap:sieve</dd>
<dt>Interoperable OAuth Scopes:</dt> <dd>urn:ietf:params:oauth:scope:mail</dd>
</dl>

<dl newline="false" spacing="compact">
<dt>Capability Name:</dt> <dd>urn:ietf:params:jmap:principals</dd>
<dt>Interoperable OAuth Scopes:</dt> <dd>urn:ietf:params:oauth:scope:mail, urn:ietf:params:oauth:scope:contacts, urn:ietf:params:oauth:scope:calendars</dd>
</dl>

<dl newline="false" spacing="compact">
<dt>Capability Name:</dt> <dd>urn:ietf:params:jmap:principals:owner</dd>
<dt>Interoperable OAuth Scopes:</dt> <dd>urn:ietf:params:oauth:scope:mail, urn:ietf:params:oauth:scope:contacts, urn:ietf:params:oauth:scope:calendars</dd>
</dl>

<dl newline="false" spacing="compact">
<dt>Capability Name:</dt> <dd>urn:ietf:params:jmap:contacts</dd>
<dt>Interoperable OAuth Scopes:</dt> <dd>urn:ietf:params:oauth:scope:contacts</dd>
</dl>

<dl newline="false" spacing="compact">
<dt>Capability Name:</dt> <dd>urn:ietf:params:jmap:calendars</dd>
<dt>Interoperable OAuth Scopes:</dt> <dd>urn:ietf:params:oauth:scope:calendars</dd>
</dl>

<dl newline="false" spacing="compact">
<dt>Capability Name:</dt> <dd>urn:ietf:params:jmap:principals:availability</dd>
<dt>Interoperable OAuth Scopes:</dt> <dd>urn:ietf:params:oauth:scope:calendars</dd>
</dl>

<dl newline="false" spacing="compact">
<dt>Capability Name:</dt> <dd>urn:ietf:params:jmap:webpush-vapid</dd>
<dt>Interoperable OAuth Scopes:</dt> <dd>urn:ietf:params:oauth:scope:mail, urn:ietf:params:oauth:scope:contacts, urn:ietf:params:oauth:scope:calendars</dd>
</dl>
</section>
</section>
</section>

</middle>

<back>

<references><name>Normative References</name>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.2119.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.3986.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.6125.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.6749.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.6750.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.6755.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.6819.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.7009.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.7591.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.7628.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.7636.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.8126.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.8174.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.8252.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.8414.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.8707.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.9207.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.9449.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.9700.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.9728.xml"/>
<reference anchor="WebAuthn" target="https://www.w3.org/TR/2021/REC-webauthn-2-20210408/">
  <front>
    <title>Web Authentication: An API for accessing Public Key Credentials - Level 2</title>
    <author initials="J." surname="Hodges" fullname="Jeff Hodges" role="editor">
      <organization>PayPal</organization>
      <address>
        <email>jdhodges@google.com</email>
      </address>
    </author>
    <author initials="J.C." surname="Jones" fullname="J.C. Jones" role="editor">
      <organization>Mozilla</organization>
      <address>
        <email>jc@mozilla.com</email>
      </address>
    </author>
    <author initials="M.B." surname="Jones" fullname="Michael B. Jones" role="editor">
      <organization>Microsoft</organization>
      <address>
        <email>mbj@microsoft.com</email>
        <uri>http://self-issued.info/</uri>
      </address>
    </author>
    <author initials="A." surname="Kumar" fullname="Akshay Kumar" role="editor">
      <organization>Microsoft</organization>
      <address>
        <email>akshayku@microsoft.com</email>
      </address>
    </author>
    <author initials="E." surname="Lundberg" fullname="Emil Lundberg" role="editor">
      <organization>Yubico</organization>
      <address>
        <email>emil@yubico.com</email>
      </address>
    </author>
    <date day="8" month="April" year="2021"/>
  </front>
  <refcontent>W3C Recommendation</refcontent>
</reference>
</references>

<references><name>Informative References</name>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.1939.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.4791.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.6352.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.6409.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.8314.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.8620.xml"/>
<xi:include href="https://xml2rfc.ietf.org/public/rfc/bibxml/reference.RFC.9051.xml"/>
<reference anchor="OpenID.Core" target="https://openid.net/specs/openid-connect-core-1_0.html">
  <front>
    <title>OpenID Connect Core 1.0</title>
    <author fullname="Nat Sakimura" initials="N." surname="Sakimura">
      <organization abbrev="NAT.Consulting (was at NRI)">NAT.Consulting</organization>
    </author>
    <author fullname="John Bradley" initials="J." surname="Bradley">
      <organization abbrev="Yubico (was at Ping Identity)">Yubico</organization>
    </author>
    <author fullname="Michael B. Jones" initials="M.B." surname="Jones">
      <organization abbrev="Self-Issued Consulting (was at Microsoft)">Self-Issued Consulting</organization>
    </author>
    <author fullname="Breno de Medeiros" initials="B." surname="de Medeiros">
      <organization abbrev="Google">Google</organization>
    </author>
    <author fullname="Chuck Mortimore" initials="C." surname="Mortimore">
      <organization abbrev="Disney (was at Salesforce)">Disney</organization>
    </author>
    <date day="15" month="December" year="2023"/>
  </front>
</reference>
<reference anchor="OpenID.Discovery" target="https://openid.net/specs/openid-connect-discovery-1_0.html">
  <front>
    <title>OpenID Connect Discovery 1.0</title>
    <author fullname="Nat Sakimura" initials="N." surname="Sakimura">
      <organization abbrev="NAT.Consulting (was at NRI)">NAT.Consulting</organization>
    </author>
    <author fullname="John Bradley" initials="J." surname="Bradley">
      <organization abbrev="Yubico (was at Ping Identity)">Yubico</organization>
    </author>
    <author fullname="Michael B. Jones" initials="M.B." surname="Jones">
      <organization abbrev="Self-Issued Consulting (was at Microsoft)">Self-Issued Consulting</organization>
    </author>
    <author fullname="Edmund Jay" initials="E." surname="Jay">
      <organization abbrev="Illumila">Illumila</organization>
    </author>
    <date day="15" month="December" year="2023"/>
  </front>
</reference>
<reference anchor="IANA.OAuth.Parameters" target="https://www.iana.org/assignments/oauth-parameters">
  <front>
    <title>OAuth Parameters</title>
    <author><organization>IANA</organization></author>
  </front>
</reference>
<reference anchor="IANA.JOSE.ALGS" target="https://www.iana.org/assignments/jose">
  <front>
    <title>JSON Web Signature and Encryption Algorithms</title>
    <author><organization>IANA</organization></author>
  </front>
</reference>
</references>

</back>

</rfc>
